name = "mangoi-ai-avatar"
compatibility_date = "2024-11-01"
pages_build_output_dir = "public"

# 환경변수(시크릿)는 코드/깃허브에 적지 않습니다.
# 로컬 테스트:   npx wrangler pages dev public   (.dev.vars 의 GEMINI_API_KEY 사용)
# 운영 배포 키:  Cloudflare 대시보드 → Pages → Settings → Environment variables → GEMINI_API_KEY
#         또는:  npx wrangler pages secret put GEMINI_API_KEY
